'use strict';

const ROUTER_INSTRUCTIONS = `ENGINEERING CYBERNETICS ROUTER

Before planning or acting, decide whether the current task is a control problem. Use engineering cybernetics only when most of these are true:
- a target state must be maintained or improved;
- state changes over time or under disturbance;
- current state can be observed;
- available actions can change the result;
- the result can be measured again and corrected.

Do not use it for a one-off, low-risk task with no evolving state. If it applies, load the engineering-cybernetics skill before acting, select one primary scenario, define objective, constraints, state, observations, actions, disturbances, feedback, and safe state, then complete the smallest verifiable loop without expanding scope. Separate actual, quoted, estimated, inferred, and unknown.`;

module.exports = { ROUTER_INSTRUCTIONS };
